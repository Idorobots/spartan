(letrec ((foo (lambda (c)
                (display "Counter: ")
                (display c)
                (newline)
                (foo (raise c)))))
  (handle (foo 1)
          (lambda (e r)
            (task-info)
            (sleep 1000)
            (when (< e 10)
              (r (+ 1 e))))))
