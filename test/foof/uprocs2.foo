(letrec ((fun (lambda (text)
                (display text)
                (newline)
                (sleep 1000)
                (fun text)))
         (monitor (lambda ()
                    (task-info)
                    (sleep 333)
                    (monitor))))
  (spawn (lambda () (fun "Hello")))
  (spawn (lambda () (fun " world!")))
  (monitor))
