../test/foof/errors/body.foo(1,0): Bad `do` syntax, expected at least one non-definition expression within:
1 |(do (define _ 23))
   ^~~~~~

../test/foof/errors/body.foo(3,0): Bad `lambda` body syntax, expected at least one non-definition expression within:
3 |(lambda ()
   ^~~~~~

../test/foof/errors/body.foo(8,0): Bad `let` body syntax, expected at least one non-definition expression within:
8 |(let ((x 23))
   ^~~~~~

../test/foof/errors/body.foo(13,0): Bad `letrec` body syntax, expected at least one non-definition expression within:
13 |(letrec ((x 23))
    ^~~~~~

Compilation aborted due to 4 errors.
