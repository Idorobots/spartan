(let ((pid (spawn (lambda ()
                    (let ((msg (recv)))
                      (display (cdr msg))
                      (send (car msg) " world!"))))))
  (send pid (cons (self) "Hello"))
  (sleep 100)
  (display (recv))
  (newline))
