(letrec ((fun (lambda (text count)
                (unless (= count 0)
                  (display text)
                  (newline)
                  (sleep 100)
                  (fun text (- count 1))))))
  (spawn (lambda ()
           (fun "Hello world!" 10))))
