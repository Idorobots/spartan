(let ((pid (spawn (lambda ()
                    (let ((msg (recv)))
                      (display (cdr msg))
                      (newline)
                      (sleep 500)
                      (send (car msg) "world!"))))))
  (sleep 500)
  (send pid (cons (self) "Hello"))
  (display (recv))
  (newline))
