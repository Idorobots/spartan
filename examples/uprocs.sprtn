(letrec ((times (lambda (count text)
                  (unless (= count 0)
                    (display text)
                    (newline)
                    (sleep 100)
                    (times (- count 1) text)))))
  (spawn (lambda ()
           (times 10 "Hello world!"))))
